version: 0.2
phases:
  build:
    commands:
      - echo Packaging booking_app.jar file...
      - mvn clean package
  post_build:
    commands:
      - echo Build complete.
artifacts:
  files:
    - target/booking_app.jar
  discard-paths: yes